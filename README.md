# Myco

To run the simulation, use `cargo run --bin simulation --release`.

If you want to run the simulation in no-enc mode, use `cargo run --bin simulation --release --features no-enc`.

If you want to run the simulation in standard encryption mode, use `cargo run --bin simulation --release`.
