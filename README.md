# Myco

To run the simulation, use `cargo run --bin simulation --release`.

If you want to run the simulation in no-enc mode, use `cargo run --release --bin simulation --features no-enc benchmark`.

If you want to run the simulation in standard encryption mode, use `cargo run --release --bin simulation benchmark`.
